import 'mocha';
import {expect} from 'chai';
// import {decodeResistor} from '../src/ejercicio-1';

describe('Ejercicio-1', () => {
  it('decodeResistor("Marrón-Verde")', () => {
    expect('15').to.be.equal('15');
  });
});
